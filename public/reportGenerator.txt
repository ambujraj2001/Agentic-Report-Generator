Generate a COMPLETE, self-contained HTML report using the blueprint and QUERY RESULTS below.

CRITICAL RULES:
- Start with <!DOCTYPE html> and end with </html>. Every tag must be closed.
- All CSS inside one <style> tag in <head>. No external resources.
- USE ONLY the real numbers from the QUERY RESULTS. Do NOT invent data.
- Keep the HTML under 250 lines. Be concise — use short text, no filler.
- No copyright, no "generated by AI", no dark backgrounds.

CSS — copy this verbatim into your <style>, do not add more:

:root{--blue:#2563eb;--green:#10b981;--orange:#f59e0b;--red:#ef4444;--bg:#f8fafc;--card:#fff;--border:#e2e8f0;--text:#1e293b;--muted:#64748b}
*{margin:0;padding:0;box-sizing:border-box}
body{font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',sans-serif;background:var(--bg);color:var(--text);line-height:1.6}
.container{max-width:1200px;margin:0 auto;padding:24px}
.header{background:linear-gradient(135deg,var(--blue),#1d4ed8);color:#fff;padding:32px;border-radius:12px;text-align:center;margin-bottom:32px}
.header h1{font-size:2rem;font-weight:700}
.header p{opacity:.85;margin-top:8px}
.grid{display:grid;gap:24px;margin-bottom:32px}
.grid-4{grid-template-columns:repeat(auto-fit,minmax(200px,1fr))}
.card{background:var(--card);border-radius:12px;padding:24px;box-shadow:0 1px 3px rgba(0,0,0,.08)}
.card h3{font-size:1rem;color:var(--muted);font-weight:500;margin-bottom:8px}
.card .value{font-size:2rem;font-weight:700}
.section{margin-bottom:32px}
.section h2{font-size:1.4rem;font-weight:600;margin-bottom:12px;padding-bottom:6px;border-bottom:2px solid var(--blue)}
table{width:100%;border-collapse:collapse;background:var(--card);border-radius:8px;overflow:hidden}
th,td{padding:10px 14px;text-align:left;border-bottom:1px solid var(--border)}
th{background:var(--bg);font-weight:600;font-size:.85rem;text-transform:uppercase}
.insight-box{background:#eff6ff;border-left:4px solid var(--blue);padding:12px 16px;border-radius:0 8px 8px 0;margin-bottom:12px}

STRUCTURE (follow this order, keep each section short):

1. HEADER — Title and subtitle with total record count
2. KPI CARDS — 4-5 cards in a div.grid.grid-4, each card has h3 (label) and div.value (number)
3. BREAKDOWN TABLE — One table with the most interesting breakdown from query results
4. KEY INSIGHTS — 3 short insight-box paragraphs
5. RECOMMENDATIONS — 3 short bullet points in a simple list

**BLUEPRINT:**